---
phase: 03-core-fan-experience
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - website/src/components/DiscographyCard.astro
  - website/src/pages/index.astro
autonomous: true
requirements: [MUSIC-01, MUSIC-02, FAN-03]

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Below the hero, a section headed 'The Catalog' shows Randevu, Brave and Open Wide as cards with cover art, title, year and feature credit — single column below 768px, three columns from 768px"
    - "Discography cards appear in releases.ts array order with the isLatest release excluded — Randevu, Brave, Open Wide — never sorted by year, title or platform count"
    - "Each card's icon row shows platforms[0], platforms[1] and platforms[2] in array order, with no sorting, no de-duplication and no reordering to favour platforms that happen to have a brand mark"
    - "A card platform whose platformIcons entry is null renders the neutral ExternalLink fallback glyph in its slot and still links to its real URL — it is never skipped, and the next platform in the array is never promoted into its place"
    - "Each card renders exactly min(3, platforms.length) icon links plus one 'All platforms' link to that release's listen page — a release with fewer than three platforms would render fewer icons rather than padding or erroring"
    - "No discography card renders an embed, a player, or an iframe — back catalog links out natively only"
    - "Adjacent platform-icon links inside a card have non-intersecting bounding boxes and each measures at least 44px on both axes at 375px; adjacent grid cards never overlap at 375, 768 or 1440"
    - "Every card exposes two distinct accessible names for its two links to the same listen page — the art-and-title link reads '{title} — listen and platforms' and the text link reads 'All {title} platforms' — so assistive tech never announces two identical ambiguous links"
    - "The header and footer social rows carry working anchors for the Spotify artist profile, Instagram, TikTok and YouTube on every page of the site, as plain icon links with no vendor follow widget or embed"
    - "The homepage composition leaves an explicit, named insertion point between the discography section and the footer for Phase 4's newsletter section"
  artifacts:
    - path: "website/src/components/DiscographyCard.astro"
      provides: "One <article> per back-catalog release: art+title link, year and credit line, first-three platform icon row, all-platforms link"
      contains: "platformIcons"
    - path: "website/src/pages/index.astro"
      provides: "'The Catalog' section iterating releases.filter(r => !r.isLatest), plus the reserved Phase 4 newsletter slot"
      contains: "The Catalog"
  key_links:
    - from: "website/src/pages/index.astro"
      to: "website/src/components/DiscographyCard.astro"
      via: "releases.filter(r => !r.isLatest).map(...) renders one card per back-catalog release"
      pattern: "DiscographyCard"
    - from: "website/src/components/DiscographyCard.astro"
      to: "website/src/pages/listen/[slug].astro"
      via: "two distinctly-labelled anchors to /listen/{release.slug}"
      pattern: "/listen/\\$\\{"
    - from: "website/src/components/DiscographyCard.astro"
      to: "website/src/data/platform-icons.ts"
      via: "platformIcons[link.platform] resolves each of the first three platforms to a mark or the fallback"
      pattern: "platformIcons\\["
    - from: "website/src/components/Header.astro"
      to: "website/src/data/releases.ts"
      via: "socials.map(...) — the FAN-03 follow anchors, verified not rebuilt"
      pattern: "socials"
  prohibitions:
    - requirement_id: "MUSIC-02"
      category: transparency
      statement: "MUST NOT present a discography card's three-icon row as if it were the complete set of places to hear a release — every card must carry a visible, separately-labelled path to that release's full platform list"
    - requirement_id: "MUSIC-01"
      category: transparency
      statement: "MUST NOT substitute, approximate, or hand-draw a brand mark for a card platform that has no verified simple-icons export, and MUST NOT silently drop that platform from the icon row to avoid showing a fallback glyph"
    - requirement_id: "FAN-03"
      category: values
      statement: "MUST NOT replace the plain social follow anchors with a vendor follow widget, embed, or any script-loading third-party button — the fan must be able to follow DARLNG without a tracking script executing"
  assumptions:
    - "EDGE (MUSIC-01, unclassified, unresolved): the spec-less edge probe could not classify MUSIC-01 into a shape category and left it unresolved. Planner judgement: the back catalog is a fixed, hand-authored, fully-typed set of exactly three releases with no pagination, no filtering and no user-generated content, so the empty/partial/zero-one-many shapes that would normally apply to a collection are structurally unreachable. The MUSIC-02 adjacency, empty and ordering rows above carry the collection behaviour that IS testable. Surfaced, not dropped."
    - "EDGE (FAN-03, unclassified, unresolved): the spec-less edge probe could not classify FAN-03 into a shape category and left it unresolved. Planner judgement: FAN-03 was already satisfied by Phase 2's header and footer social rows; this phase verifies rather than rebuilds them, so no new data-shape edge is introduced. The verification truth above is the checkable residual. Surfaced, not dropped."
---

## Phase Goal

**Source:** `.planning/ROADMAP.md` Phase 3 `**Goal:**` line — quoted in full in `03-01-PLAN.md`. Not in user-story form; see the MVP_MODE note there. This plan delivers the goal's "browse the back-catalog discography" and "follow DARLNG on all social platforms" clauses.

<objective>
Complete the fan experience: a "The Catalog" grid of the three back-catalog releases below the hero, each card linking out natively to streaming platforms and through to its listen page; a verification pass confirming Phase 2's header and footer follow anchors genuinely satisfy FAN-03; and the final whole-phase browser sweep across every page at every tested viewport.

This is the last expansion from the Plan 01 tracer. It also reserves the homepage slot Phase 4's newsletter section will occupy.

Purpose: MUSIC-01, MUSIC-02, FAN-03, and the phase-wide evidence the verifier needs.
Output: `DiscographyCard.astro`, the catalog section in `index.astro`, a FAN-03 and link-invariant audit, and the full screenshot set.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-fan-experience/03-UI-SPEC.md
@.planning/phases/03-core-fan-experience/03-RESEARCH.md
@.planning/phases/03-core-fan-experience/03-01-SUMMARY.md
@.planning/phases/03-core-fan-experience/03-02-SUMMARY.md
@website/src/data/releases.ts
@website/src/components/Header.astro
@website/src/components/Footer.astro
@website/src/pages/index.astro

<interfaces>
<!-- Contracts the executor needs. Do NOT go exploring for these. -->

From `website/src/data/releases.ts` — the three back-catalog entries, with the first three `platforms[]` entries that drive each card's icon row:
- `randevu` (2024, "Darlng ft. Shubi Di Badman", 7 platforms) — first three: `spotify`, `deezer`, `amazonMusic`. **`amazonMusic` has no brand mark** and must render the fallback glyph in slot 3.
- `brave` (2020, "Darlng ft. Ray Pineapple", 5 platforms) — first three: `spotify`, `appleMusic`, `tidal`. All three have marks.
- `open-wide` (2019, "Darlng ft. Don Classic", 6 platforms) — first three: `spotify`, `appleMusic`, `amazonMusic`. **Slot 3 renders the fallback.**
- `artistLine` already contains the full credit string (for example `Darlng ft. Shubi Di Badman`) — the card's credit line derives from it; do not re-concatenate a second `ft.`.
- Cover sources are 2450x2450 and 3000x3000, far above the requested card widths, so no upscale risk.

From `website/src/data/platform-icons.ts` (Plan 01): `platformIcons: Record<Platform, SimpleIcon | null>` — `null` for `amazonMusic`, `anghami`, `boomplay`.

From `website/src/components/BrandIcon.astro`: `{ icon: SimpleIcon; class?: string }`, inline SVG, `currentColor` only.

From `@lucide/astro`: `ChevronRight`, `ExternalLink` — props `size`, `class`.

From `website/src/components/Header.astro` and `Footer.astro` (Phase 2, **do not modify**): both render `socials.map(...)` into anchors with `aria-label={`Follow DARLNG on ${social.label}`}`, `target="_blank"`, `rel="noopener noreferrer"`, `min-h-11 min-w-11`. `socials` order is Spotify, Instagram, Facebook, YouTube, TikTok. FAN-03 names Spotify artist profile, Instagram, TikTok and YouTube — all four are present; Facebook is an extra, not a violation.

From `website/src/layouts/Layout.astro`: `<main id="main" class="mx-auto w-full max-w-7xl px-4 md:px-6 xl:px-8">`. The discography section sits inside this container normally — unlike the hero, it does **not** use negative gutter margins.

Tailwind v4 note: the `hover:` variant is already wrapped in `@media (hover: hover)`, so card hover effects are pointer-only without extra work.

Astro `compressHTML` collapses built HTML onto very few physical lines. Count occurrences with `grep -o PATTERN FILE | wc -l`, never `grep -c` — `grep -c` counts lines and undercounts, as documented in `02-02-SUMMARY.md`.
</interfaces>
</context>

<!-- planner-discipline-allow: open.spotify.com/follow -->
<!-- planner-discipline-allow: spotify.com/embed -->
<!-- planner-discipline-allow: <iframe -->

<tasks>

<task type="auto">
  <name>Task 1: DiscographyCard component and 'The Catalog' grid</name>
  <reversibility rating="reversible">A presentational component over existing typed data behind a one-prop interface; restructuring the card later touches one file plus its single call site.</reversibility>
  <read_first>
    - `.planning/phases/03-core-fan-experience/03-UI-SPEC.md` — section "Discography (MUSIC-01, MUSIC-02)": heading, section padding, grid, card anatomy, the two-links rationale, the declined year/genre chips, hover state
    - `.planning/phases/03-core-fan-experience/03-RESEARCH.md` — "Code Examples" (discography card link pattern), "Pitfall 4"
    - `website/src/data/releases.ts`
    - `website/src/data/platform-icons.ts`
    - `website/src/pages/index.astro`
  </read_first>
  <files>website/src/components/DiscographyCard.astro, website/src/pages/index.astro</files>
  <action>
Create `src/components/DiscographyCard.astro` with `interface Props { release: Release }` (import the `Release` type from `../data/releases`). Render a single `<article>` per 03-UI-SPEC.md's card anatomy:

1. A `class="group block"` anchor to `/listen/{release.slug}` with `aria-label={`${release.title} — listen and platforms`}`, containing the cover `<Picture>` (`formats={['avif','webp']}`, `widths={[300,400,600,800]}`, `sizes="(min-width:1280px) 384px, (min-width:768px) 224px, 100vw"`, `fit="cover"`, `loading="lazy"`, `alt={`${release.title} cover art`}`, classes `aspect-square w-full rounded-card object-cover transition-transform duration-200 group-hover:scale-105`) inside a `relative overflow-hidden rounded-card transition-shadow duration-200 group-hover:shadow-[0_8px_24px_rgba(0,0,0,0.4)]` wrapper, followed by `<h3 class="mt-4 text-2xl leading-tight font-display font-bold transition-colors duration-200 group-hover:text-accent">{release.title}</h3>`.
2. A credit line `<p class="mt-1 text-sm text-text-muted">` reading `{release.year} · {credit}` where `credit` is the feature clause taken from `release.artistLine` — derive it in frontmatter rather than hardcoding three strings, and do not emit a second `ft.` prefix.
3. A platform icon row: take `release.platforms.slice(0, 3)` — `slice` naturally yields fewer than three for a shorter array, which is the intended behaviour. Render each entry **in array order** as an `<a>` with the real `url`, `target="_blank"`, `rel="noopener noreferrer"`, `aria-label={`Listen to ${release.title} on ${link.label}`}`, classes `flex items-center justify-center min-h-11 min-w-11 rounded-full text-text-muted transition-colors duration-150 hover:text-accent focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-accent active:scale-95`, containing `BrandIcon` at `w-5 h-5` when `platformIcons[link.platform]` resolved to a mark, otherwise `ExternalLink` at `size={20}`. Never filter, sort, or skip an entry because its lookup returned null — the third slot on Randevu and Open Wide is Amazon Music and it renders the fallback glyph.
4. An `<a>` to the same `/listen/{release.slug}` with `aria-label={`All ${release.title} platforms`}`, visible text `All platforms` followed by `<ChevronRight size={16} />` (the icon is the arrow — do not also emit an arrow character), styled `text-sm font-semibold text-accent inline-flex items-center gap-1`. The two distinct aria-labels are what keep assistive tech from announcing two identical ambiguous links to the same URL.

No year or genre chips — 03-UI-SPEC.md declined them deliberately; the year lives inline in the credit line. No embed, no player, no iframe on any card.

Then in `src/pages/index.astro`, below the hero section and inside `<main>`'s normal container (no negative gutter margins here), add `<section class="py-16" aria-labelledby="catalog-heading">` containing `<h2 id="catalog-heading" class="text-2xl leading-tight font-display font-bold">The Catalog</h2>` and a `grid grid-cols-1 md:grid-cols-3 gap-8 mt-8` that maps `releases.filter((release) => !release.isLatest)` to `DiscographyCard`. Iterate the filtered array directly — no sort, no reverse, no slice.

Immediately after that section and before the closing `</Layout>`, leave the Phase 4 insertion point: an HTML comment naming it as the slot where the newsletter section mounts in Phase 4, with nothing else between it and the layout close. Keep it a comment only — no placeholder markup, no empty section element.

The old skeleton tagline material ("Independent. No schedule." and the token swatch grid) stays removed — 03-UI-SPEC.md's discretionary log dropped it as redundant once the hero carries brand identity.
  </action>
  <acceptance_criteria>
    - `website/src/components/DiscographyCard.astro` exists and `grep -c 'DiscographyCard' website/src/pages/index.astro` is at least 2 (import plus usage).
    - `grep -c 'slice(0, 3)' website/src/components/DiscographyCard.astro` is at least 1 and `grep -cE '\.(sort|reverse)\(' website/src/components/DiscographyCard.astro` is 0.
    - `grep -c 'isLatest' website/src/pages/index.astro` is at least 1 — the filter is present.
    - `cd website && npm run check` exits 0 and `cd website && npm run build` exits 0.
    - `grep -o 'The Catalog' website/dist/index.html | wc -l` is at least 1.
    - `grep -o '<article' website/dist/index.html | wc -l` is exactly 3.
    - `grep -o '— listen and platforms' website/dist/index.html | wc -l` is exactly 3 and `grep -o 'All platforms' website/dist/index.html | wc -l` is at least 3.
    - `grep -o 'href="/listen/randevu"' website/dist/index.html | wc -l` is exactly 2; same for `/listen/brave` and `/listen/open-wide`.
    - `grep -o 'aria-label="Listen to Randevu on' website/dist/index.html | wc -l` is exactly 3; same count for Brave and Open Wide.
    - `grep -o 'https://music.amazon.com/albums/B0D8LHNXQ6?trackAsin=B0D8LJSBW4' website/dist/index.html | wc -l` is at least 1 — the icon-less third slot still links out.
    - `grep -o 'https://www.amazon.com/gp/product/B082RBHFWH' website/dist/index.html | wc -l` is at least 1.
    - `grep -o '<iframe' website/dist/index.html | wc -l` is 0 — still no embed anywhere in the static homepage.
    - `grep -ci 'newsletter' website/src/pages/index.astro` is at least 1 — the Phase 4 slot is named.
    - `grep -o 'Independent. No schedule.' website/dist/index.html | wc -l` is 0 — the skeleton copy is gone.
    - `git diff --exit-code website/src/components/Header.astro website/src/components/Footer.astro website/scripts/check-contrast.mjs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd website && npm run check && npm run build && node -e "const fs=require('fs');const h=fs.readFileSync('dist/index.html','utf8');const c=(re)=>(h.match(re)||[]).length;if(c(/<article/g)!==3)throw new Error('expected 3 discography cards, got '+c(/<article/g));if(c(/— listen and platforms/g)!==3)throw new Error('art+title aria-labels wrong: '+c(/— listen and platforms/g));for(const s of ['randevu','brave','open-wide']){if(c(new RegExp('href=\"/listen/'+s+'\"','g'))!==2)throw new Error(s+' does not have exactly two listen links');}for(const t of ['Randevu','Brave','Open Wide']){const n=c(new RegExp('aria-label=\"Listen to '+t+' on','g'));if(n!==3)throw new Error(t+' icon row rendered '+n+' links, expected 3');}if(!h.includes('https://music.amazon.com/albums/B0D8LHNXQ6?trackAsin=B0D8LJSBW4'))throw new Error('icon-less platform dropped from Randevu card');if(!h.includes('https://www.amazon.com/gp/product/B082RBHFWH'))throw new Error('icon-less platform dropped from Open Wide card');if(h.includes('<iframe'))throw new Error('an iframe shipped in the static homepage');if(h.includes('Independent. No schedule.'))throw new Error('skeleton copy still present');console.log('CATALOG OK');"</automated>
  </verify>
  <done>Three back-catalog cards render below the hero in catalog order with cover art, title, year, credit, three in-order platform icon links (fallback glyph included) and a distinctly-labelled all-platforms link; the Phase 4 newsletter slot is reserved.</done>
</task>

<task type="auto">
  <name>Task 2: FAN-03 audit and phase-wide external-link invariant gate</name>
  <read_first>
    - `.planning/REQUIREMENTS.md` — the FAN-03 line and the "Spotify Follow Button widget" row of the Out of Scope table
    - `website/src/components/Header.astro`
    - `website/src/components/Footer.astro`
    - `website/src/data/releases.ts` — the `socials` array
    - `.planning/phases/02-brand-data-base-layout/02-02-SUMMARY.md` — the Phase 2 count-match methodology this extends
  </read_first>
  <files>website/src/pages/index.astro</files>
  <action>
This is a verification task. FAN-03 was satisfied by Phase 2's header and footer social rows — **verify** them against the requirement, do not rebuild them, and do not modify `Header.astro` or `Footer.astro`. Only touch `index.astro` if the audit turns up a genuine defect attributable to this phase's work.

Run the audit against the freshly built `website/dist/` across all six emitted HTML files — `index.html`, `404.html`, and the four `listen/*/index.html` — using occurrence counting (`grep -o PATTERN FILE | wc -l`), never `grep -c`. Do not wrap any count in an error-suppressing fallback; a missing file must fail the gate loudly rather than compare as zero.

**FAN-03 presence.** In each of the six files, confirm every one of the four requirement-named profiles appears at least twice (once in the header row, once in the footer row): the Spotify artist URL `https://open.spotify.com/artist/0uXxSPfLr36OuyGDKiBzV3`, `https://www.instagram.com/darlngmusic`, `https://www.tiktok.com/@darlng_music`, and `https://www.youtube.com/channel/UC_noHUKnp2wJ3fi29smmZKw`. Confirm `aria-label="Follow DARLNG on` appears exactly ten times per file (five platforms in each of two rows).

**FAN-03 no-widget.** Confirm no vendor follow widget or player embed reached any page: the literals `open.spotify.com/follow` and `spotify.com/embed` must each have an occurrence count of zero in all six files, and `<iframe` must have an occurrence count of zero in all six.

**External-link invariant.** For each of the six files, assert the occurrence count of `target="_blank"` equals the occurrence count of `rel="noopener noreferrer"`. This extends Phase 2's security-audited pairing to the roughly 40 links this phase added. Report the per-file counts in the SUMMARY so the growth from Phase 2's baseline of 10 is visible and explained.

**No invented logos.** Assert that the only `simple-icons` brand marks reaching the built pages are the ones `platform-icons.ts` and `social-icons.ts` actually map. A practical check: confirm the count of card and button icon slots that fell back to the neutral glyph is exactly what the data predicts — three fallback slots on the homepage would be wrong; the data predicts two (Randevu slot 3 and Open Wide slot 3), plus every `amazonMusic`, `anghami` and `boomplay` entry across the four listen pages. Compute the expected number from `releases.ts` rather than hardcoding it, and state both expected and actual in the SUMMARY.

Write the audit as a single Node script executed inline so the whole gate passes or fails atomically. If any assertion fails, fix the cause in this phase's own files and re-run; if the cause is a Phase 2 file, report it in the SUMMARY as a finding rather than editing Phase 2 components silently.
  </action>
  <acceptance_criteria>
    - The audit script exits 0.
    - In all six built HTML files, the occurrence count of `aria-label="Follow DARLNG on` is exactly 10.
    - In all six files, each of the four FAN-03 profile URLs has an occurrence count of at least 2.
    - In all six files, the occurrence counts of `open.spotify.com/follow`, `spotify.com/embed` and `<iframe` are each 0.
    - In all six files, the occurrence count of `target="_blank"` equals the occurrence count of `rel="noopener noreferrer"`.
    - The computed expected fallback-glyph count matches the actual count, with both figures recorded in the SUMMARY.
    - `git diff --exit-code website/src/components/Header.astro website/src/components/Footer.astro` exits 0 — FAN-03 was verified, not rebuilt.
  </acceptance_criteria>
  <verify>
    <automated>cd website && node -e "const fs=require('fs');const files=['dist/index.html','dist/404.html','dist/listen/eseriani/index.html','dist/listen/randevu/index.html','dist/listen/brave/index.html','dist/listen/open-wide/index.html'];const profiles=['https://open.spotify.com/artist/0uXxSPfLr36OuyGDKiBzV3','https://www.instagram.com/darlngmusic','https://www.tiktok.com/@darlng_music','https://www.youtube.com/channel/UC_noHUKnp2wJ3fi29smmZKw'];const esc=s=>s.replace(/[.*+?^\${}()|[\]\\\\]/g,'\\\\\$&');for(const f of files){const h=fs.readFileSync(f,'utf8');const c=p=>(h.match(new RegExp(esc(p),'g'))||[]).length;const follow=c('aria-label=\"Follow DARLNG on');if(follow!==10)throw new Error(f+': expected 10 follow anchors, got '+follow);for(const p of profiles){if(c(p)<2)throw new Error(f+': FAN-03 profile under-represented: '+p);}for(const bad of ['open.spotify.com/follow','spotify.com/embed','<iframe']){if(c(bad)!==0)throw new Error(f+': forbidden marker present: '+bad);}const t=c('target=\"_blank\"'),r=c('rel=\"noopener noreferrer\"');if(t!==r)throw new Error(f+': target/rel mismatch '+t+'/'+r);console.log(f+'  follow=10  blank='+t+'  rel='+r);}console.log('FAN-03 + LINK INVARIANT OK');" && git diff --exit-code src/components/Header.astro src/components/Footer.astro</automated>
  </verify>
  <done>FAN-03 is verified satisfied by the existing Phase 2 header and footer rows on every page; the `target="_blank"` / `rel="noopener noreferrer"` pairing invariant holds across all six built pages including this phase's ~40 new links; no vendor widget, embed, or invented logo reached the build.</done>
</task>

<task type="auto">
  <name>Task 3: Full-phase browser sweep at 375/768/1440 across homepage and listen pages</name>
  <read_first>
    - `.planning/phases/03-core-fan-experience/03-UI-SPEC.md` — "Discography" grid and hover state, "UI Considerations" table
    - `.planning/phases/03-core-fan-experience/03-01-SUMMARY.md` and `03-02-SUMMARY.md` — the assertions already proven, so this task extends rather than repeats them
    - `website/src/pages/index.astro`
    - `website/src/components/DiscographyCard.astro`
  </read_first>
  <files>website/src/pages/index.astro, website/src/components/DiscographyCard.astro</files>
  <action>
Final whole-phase verification in a real browser. Invoke the `agent-browser` skill against `npm run preview` from `website/` (executors have no `mcp` preview tools — use `npx agent-browser`). Set viewports with `agent-browser set viewport W H`; run every assertion through `agent-browser eval --stdin` heredocs; write screenshots into `/tmp/darlng-phase3/`.

At **375x812, 768x1024 and 1440x900** on `/`:
1. **Grid geometry.** Assert the catalog grid's computed `grid-template-columns` resolves to one track at 375 and three tracks at 768 and 1440. Assert the three `<article>` rects do not intersect each other at any viewport.
2. **Card tap targets and adjacency.** For each card, assert its three platform-icon anchors each report a bounding box ≥44px on both axes and that adjacent icon rects do not intersect.
3. **Whole-page overflow.** Assert `document.documentElement.scrollWidth === document.documentElement.clientWidth`.
4. **LCP still the hero.** Re-run the `PerformanceObserver` largest-contentful-paint check with the full page composed and assert the entry element is still the `#hero picture img` — adding the catalog's lazy images must not have displaced it.
5. **Lazy below the fold.** Assert every `img` inside the catalog section reports `loading === 'lazy'`, and that the hero image does not.
6. **Distinct link names.** For each card, assert the two anchors pointing at the same `/listen/{slug}` expose different accessible names.
7. **Phase 4 slot.** Assert the discography section is the last content section before the footer, so the reserved newsletter slot sits between them.

At the same three viewports on `/listen/eseriani`, `/listen/brave` and `/listen/open-wide`:
8. Assert the platform button counts are 4, 5 and 6 respectively; that all buttons are ≥44px tall with non-intersecting rects; that `scrollWidth === clientWidth`; and that the back link resolves to `/`.

Capture `catalog-375.png`, `catalog-768.png`, `catalog-1440.png` (scrolled to the catalog section), `full-1440.png` (`screenshot --full` of the whole homepage), and `listen-brave-375.png`, `listen-open-wide-1440.png`.

If any assertion fails, fix `index.astro` or `DiscographyCard.astro`, rebuild, and re-assert until green. Record every fix in the SUMMARY.

Close out the phase in the SUMMARY with an explicit backstop-evidence section covering the three items that are not machine-decidable: hero scrim legibility over the photographic artwork, hero-art softness on displays wider than the 1254px source, and the post-click embed failure path with an ad blocker. State the evidence gathered for each, or state plainly that it needs a human — never imply a silent pass.
  </action>
  <acceptance_criteria>
    - `/tmp/darlng-phase3/catalog-375.png`, `catalog-768.png`, `catalog-1440.png`, `full-1440.png`, `listen-brave-375.png`, `listen-open-wide-1440.png` all exist.
    - Grid assertion output shows 1 track at 375 and 3 tracks at 768 and 1440, with non-intersecting `<article>` rects.
    - Every card platform-icon anchor reports a rect ≥44px on both axes with no intersections at 375.
    - `scrollWidth === clientWidth` at 375, 768 and 1440 on `/`, `/listen/eseriani`, `/listen/brave` and `/listen/open-wide`.
    - LCP assertion output shows the element is still `#hero picture img` at all three viewports with the full page composed.
    - Every catalog-section `img` reports `loading === "lazy"`; the hero image does not.
    - Each card's two same-destination anchors report different accessible names.
    - Platform button counts are 4 on eseriani, 5 on brave, 6 on open-wide, all ≥44px tall with non-intersecting rects.
    - `cd website && npm run check` and `cd website && npm run build` both exit 0 after any fixes.
    - The SUMMARY contains an explicit backstop-evidence section naming all three backstop items and the evidence or human-needed status for each.
  </acceptance_criteria>
  <verify>
    <automated>ls /tmp/darlng-phase3/catalog-375.png /tmp/darlng-phase3/catalog-768.png /tmp/darlng-phase3/catalog-1440.png /tmp/darlng-phase3/full-1440.png /tmp/darlng-phase3/listen-brave-375.png /tmp/darlng-phase3/listen-open-wide-1440.png && cd website && npm run check && npm run build</automated>
    <human-check>Open `full-1440.png` and `catalog-375.png`. Confirm the page reads as one cinematic composition — the hero dominates, the catalog reads as a quiet supporting section rather than competing with it, the accent is used decisively but sparingly, and nothing looks like an unstyled default.</human-check>
  </verify>
  <done>Every mechanical assertion passes across the homepage and three listen pages at all three viewports; the LCP element survives the full composition; the complete screenshot set is on disk; the three backstop items are explicitly reported rather than silently passed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| darlng.com → streaming platforms | Nine new card icon links plus the two-per-card listen-page links; every outbound anchor is a third-party hand-off |
| darlng.com → social platforms | Phase 2's ten follow anchors per page, re-verified here rather than rebuilt |
| build-time `releases.ts` → rendered cards | Titles, years, credit lines and URLs are typed build-time constants; no runtime or user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | Every `target="_blank"` anchor across all six built pages | high | mitigate | Task 2's audit asserts an exact occurrence-count match between `target="_blank"` and `rel="noopener noreferrer"` in `index.html`, `404.html` and all four listen pages, extending Phase 2's audited invariant to this phase's ~40 new links and failing loudly on any file it cannot read |
| T-03-04 | Spoofing | `DiscographyCard.astro` icon row | medium | mitigate | Icons resolve only through `platformIcons`; a `null` lookup renders the neutral `ExternalLink` glyph in `--color-text-muted`, visually distinct from any brand mark. Task 2 computes the expected fallback count from the data and compares it to the built output, so a substituted or dropped mark fails the gate |
| T-03-09 | Spoofing | FAN-03 follow anchors | high | mitigate | Task 2 asserts all four requirement-named profile URLs are present at least twice on every page, that exactly ten follow anchors render per page, and that no vendor follow widget or player embed reached any page — a swapped or removed profile URL fails the gate |
| T-03-10 | Information Disclosure | Back-catalog cards | low | mitigate | MUSIC-02 mandates native links only; Task 1 and Task 2 both assert zero iframe elements in the built homepage, so no back-catalog embed can silently introduce a third-party request |
| T-03-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install task in this plan or this phase. 03-RESEARCH.md's Package Legitimacy Audit re-verified the two first-time-imported packages and resolved both `[SUS]` flags as `too-new`-heuristic false positives against official org repos. No `[ASSUMED]` or `[SLOP]` package remains |
</threat_model>

<flagged_assumptions>
- **MUSIC-01 — unclassified, unresolved (edge probe).** No shape category could be assigned. Planner judgement: the back catalog is a fixed, hand-authored, fully-typed set of exactly three releases with no pagination, filtering or user-generated content, so the empty / partial / zero-one-many shapes that would normally apply to a collection are structurally unreachable here. The MUSIC-02 adjacency, empty and ordering predicates lifted into `must_haves.truths` carry the collection behaviour that IS testable.
- **FAN-03 — unclassified, unresolved (edge probe).** No shape category could be assigned. Planner judgement: FAN-03 was already satisfied by Phase 2; this plan verifies rather than rebuilds, so no new data-shape edge is introduced. The verification truth in `must_haves` is the checkable residual.

Canon-referral breadcrumbs (surfaced, deliberately not minted as prohibitions):
- `noopener` / `noreferrer` pairing is canon — covered by `/gsd-secure-phase` and `T-03-01`; enforced by Task 2's gate, not minted.
- Generic GDPR/consent canon is covered by `/gsd-secure-phase`; the bespoke no-vendor-widget constraint IS minted above because it is a named REQUIREMENTS.md out-of-scope decision with a mechanical check.
</flagged_assumptions>

<verification>
- `cd website && npm run check` exits 0; `cd website && npm run build` exits 0.
- Built homepage contains exactly three `<article>` cards, two distinctly-labelled listen links per card, three in-order icon links per card, and zero iframe elements.
- Task 2's audit script exits 0 across all six built HTML files.
- `git diff --exit-code website/src/components/Header.astro website/src/components/Footer.astro website/scripts/check-contrast.mjs` exits 0.
- Browser assertions green at 375/768/1440 on `/`, `/listen/eseriani`, `/listen/brave`, `/listen/open-wide`.
- Full screenshot set present under `/tmp/darlng-phase3/`.
</verification>

<success_criteria>
- MUSIC-01 satisfied: Randevu, Brave and Open Wide render as a responsive grid with cover art, title, year and credit.
- MUSIC-02 satisfied: every card links out natively to streaming platforms and through to its listen page, with no embed anywhere in the back catalog.
- FAN-03 satisfied and verified: Spotify artist, Instagram, TikTok and YouTube follow anchors present on every page as plain icon links, no vendor widget.
- The homepage LCP element is still the hero image with the full page composed.
- No horizontal overflow and no sub-44px tap target on any page at any tested viewport.
- A named insertion point for Phase 4's newsletter section sits between the catalog and the footer.
</success_criteria>

<artifacts_this_plan_produces>
**New files**
- `website/src/components/DiscographyCard.astro` — props `{ release: Release }`; renders one `<article>` with art+title link, credit line, three-slot icon row, all-platforms link

**Modified files**
- `website/src/pages/index.astro` — adds the `The Catalog` section iterating `releases.filter(r => !r.isLatest)` and the reserved Phase 4 newsletter insertion comment

**Unchanged by contract**
- `website/src/components/Header.astro`, `website/src/components/Footer.astro` (FAN-03 verified, not rebuilt), `website/scripts/check-contrast.mjs`, `website/src/data/releases.ts`, `website/src/layouts/Layout.astro`

**Screenshots**: `/tmp/darlng-phase3/catalog-{375,768,1440}.png`, `/tmp/darlng-phase3/full-1440.png`, `/tmp/darlng-phase3/listen-brave-375.png`, `/tmp/darlng-phase3/listen-open-wide-1440.png`

**Phase-wide artifact inventory (all three plans)**
- New: `platform-icons.ts`, `PlatformButton.astro`, `YouTubeFacade.astro`, `DiscographyCard.astro`, `listen/[slug].astro`
- Modified: `index.astro`, `global.css`
- New exported symbols: `platformIcons` (`Record<Platform, SimpleIcon | null>`), `getStaticPaths` (in `listen/[slug].astro`)
- New CSS surface: `--text-hero` theme token, `.hero-scrim`, `.platform-button:hover`, `.facade-play:hover`, `.facade-play:focus-visible`
- New routes: `/listen/eseriani`, `/listen/randevu`, `/listen/brave`, `/listen/open-wide`
- No new assets, no new npm dependencies
</artifacts_this_plan_produces>

<output>
Create `.planning/phases/03-core-fan-experience/03-03-SUMMARY.md` when done.
</output>
