---
phase: 01-infrastructure-deploy
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - legacy/index.html
  - legacy/randevu.mp3
  - legacy/Gruntfile.js
  - legacy/package.json
  - legacy/yarn.lock
  - legacy/scss
  - legacy/js
  - legacy/css
  - legacy/fonts
  - legacy/img
  - legacy/json
  - legacy/files
  - website/DEPLOY.md
autonomous: true
requirements: [INFRA-02, INFRA-03, INFRA-04]
estimate:
  tokens: 37000
  raw_tokens: 37000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "All twelve legacy artifacts named in D-12 live under `legacy/` and none of them remain at the repo root"
    - "The relocation used `git mv`, so `git log --follow` still reaches each file's 2019 history"
    - "The repo root contains only `CLAUDE.md`, `legacy/`, `website/`, `.planning/`, `.git/` and untracked scratch files — nothing the old Grunt pipeline needs"
    - "`website/DEPLOY.md` documents every live action the user must perform: Coolify application setup (D-09/D-10), domain + TLS + redirects (D-13/D-14/D-15), Listmonk + Postgres (D-01/D-02/D-06), Resend SMTP relay (D-03/D-04), and SPF/DKIM/DMARC at LWS (D-05)"
    - "DEPLOY.md states explicitly that if an existing Coolify application already binds darlng.com, that binding must be removed or reassigned BEFORE the new application claims it — two applications must not both claim the same domain (edge: adjacency, INFRA-04)"
    - "DEPLOY.md carries a prominent warning that moving the legacy files out of the repo root will break the currently-live 2019 placeholder on the next auto-deploy from `master`, so the cutover must not be left half-finished (D-11/D-15)"
    - "DEPLOY.md instructs the user to enter every credential in Coolify's environment-variable UI or Listmonk's own settings screen — never into a file in this repository"
    - "DEPLOY.md prescribes Coolify's built-in domain Direction setting for www->apex and its Force-HTTPS setting, and records the hand-written Traefik redirect labels only as a named fallback if that setting is absent from the installed Coolify version (D-14)"
    - "DEPLOY.md instructs deploying Listmonk through Coolify's own one-click service picker rather than importing a hand-modified compose file, and documents the Resend SMTP relay values (host, port 587, STARTTLS) with a note to cross-check them against Resend's own docs at setup time (D-03)"
    - "DEPLOY.md ends with a copy-pasteable verification checklist covering roadmap success criteria 2 through 5, which the user runs after cutover"
    - statement: "DEPLOY.md's step order is executable as written — Resend domain verification precedes Listmonk SMTP configuration, and Listmonk list creation precedes recording the list UUID for Phase 4 (edge: ordering, INFRA-04)"
      verification: backstop
  artifacts:
    - path: "website/DEPLOY.md"
      provides: "The complete user-executed live-infrastructure runbook for INFRA-02/03/04"
      contains: "mail.darlng.com"
      min_lines: 90
    - path: "legacy/index.html"
      provides: "Archived 2019 placeholder entrypoint, moved with history intact"
    - path: "legacy/Gruntfile.js"
      provides: "Archived legacy build pipeline, isolated from the website/ npm toolchain"
  key_links:
    - from: "website/DEPLOY.md"
      to: "website/Dockerfile"
      via: "the runbook tells Coolify to use Build Pack = Dockerfile with Base Directory website/, which is the artifact Plan 01 produced"
      pattern: "Dockerfile"
    - from: "website/DEPLOY.md"
      to: "Phase 4 env vars"
      via: "records where PUBLIC_LISTMONK_URL and PUBLIC_LISTMONK_LIST_UUID are pasted once Listmonk is live"
      pattern: "PUBLIC_LISTMONK_LIST_UUID"
  prohibitions:
    - statement: "DEPLOY.md and the repository MUST NOT contain any live secret value — no Resend API key, no Listmonk admin password, no SMTP password. A runbook is exactly the document where a real key gets pasted 'just for now'; every credential must be described by where to obtain it and where to paste it, never by its value."
      status: unverified
    - statement: "DEPLOY.md MUST NOT present invented or generic example values as though they were the user's real account-specific records. The Resend DKIM public key and selector, and the Listmonk list UUID, are unique per account and must be marked as copy-from-dashboard placeholders — a plausible-looking fake value produces DNS records that silently never verify."
      status: unverified

flagged_assumptions:
  - "Edge probe returned INFRA-03 as `unclassified` (status: unresolved). Planner assumption: the redirect edges that matter (www vs apex, HTTP vs HTTPS, redirect loops) cannot be exercised without the live domain, which the scope revision defers to the user. They are therefore authored as runbook verification steps rather than phase acceptance criteria. Not dismissed — carried forward to the user's cutover."
  - "Edge probe returned INFRA-02 as `unclassified` (status: unresolved) — the serving-layer half is covered by Plan 01's acceptance criteria; the Coolify-configuration half is documented, not executed."
  - "RESEARCH.md refers to 'the ten legacy root artifacts', but D-12 enumerates TWELVE (index.html, randevu.mp3, scss/, js/, css/, fonts/, img/, json/, files/, Gruntfile.js, package.json, yarn.lock). D-12 is authoritative; Task 1 moves all twelve by explicit name."
  - "RESEARCH.md assumption A3 — that the installed Coolify version exposes the built-in domain Direction setting — is unverifiable from here. DEPLOY.md handles it by documenting the built-in setting as primary and the manual Traefik label approach as a named fallback."
  - "RESEARCH.md assumption A2 — Resend's exact SMTP values — is LOW-confidence WebSearch synthesis. DEPLOY.md records the values with an explicit instruction to cross-check them against Resend's own SMTP documentation before saving, so a stale value fails loudly at setup rather than silently at send time."
---

<objective>
Finish the LOCAL-ONLY half of Phase 1: make `website/` unambiguously the site by archiving the 2019 placeholder, and hand the user a runbook complete enough to execute every live step without further engineering input.

Purpose: the runbook half of INFRA-02 (Coolify application with Base Directory `website/` and the custom nginx config Plan 01 proved), plus INFRA-03 (apex/www and HTTPS with no redirect loops) and INFRA-04 (Listmonk reachable and configured with a double-opt-in fan list). Per the 2026-08-04 scope revision, all three are DOCUMENTED here and EXECUTED by the user — no live infrastructure is touched by this plan.

Output: `legacy/` containing the archived 2019 site, and `website/DEPLOY.md`.
</objective>

## Phase Goal

**Goal (verbatim from ROADMAP.md):** The Astro project builds and deploys cleanly to darlng.com via Coolify, with correct TLS/redirects, nginx cache headers, and a live Listmonk instance ready for newsletter wiring.

> MVP_MODE note: this goal is not in user-story form and one must not be invented here — run `/gsd-mvp-phase 1` if the story framing is wanted. The scope revision converts this goal's live clauses into the runbook this plan authors.

## Artifacts this phase produces

**Relocated (via `git mv`, history preserved):** `legacy/index.html`, `legacy/randevu.mp3`, `legacy/Gruntfile.js`, `legacy/package.json`, `legacy/yarn.lock`, `legacy/scss/`, `legacy/js/`, `legacy/css/`, `legacy/fonts/`, `legacy/img/`, `legacy/json/`, `legacy/files/`

**New file:** `website/DEPLOY.md`

**DEPLOY.md section contract (later phases and the user's cutover depend on these headings existing):**
- `## 1. Coolify — Static Site Application`
- `## 2. Listmonk + Postgres`
- `## 3. Resend — Domain & DNS`
- `## 4. Post-Cutover Verification`

**Values this phase records for Phase 4 to consume:** `PUBLIC_LISTMONK_URL` (`https://mail.darlng.com`) and `PUBLIC_LISTMONK_LIST_UUID` (produced when the user creates the fan list)

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-infrastructure-deploy/01-CONTEXT.md
@.planning/phases/01-infrastructure-deploy/01-RESEARCH.md
@.planning/phases/01-infrastructure-deploy/01-SKELETON.md
@.planning/phases/01-infrastructure-deploy/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Archive the 2019 placeholder into legacy/</name>
  <precondition>All twelve D-12 artifacts are present at the repo root and tracked by git — `git ls-files --error-unmatch index.html Gruntfile.js package.json yarn.lock randevu.mp3` exits 0.</precondition>
  <reversibility rating="costly">The currently-live Coolify application builds from the repo root, so once this lands, the next auto-deploy from `master` will no longer find `index.html` and the live 2019 placeholder will break until the user completes the cutover. Undoing the move itself is a cheap `git mv` back, but the live consequence is why the cutover warning in Task 2 is mandatory.</reversibility>
  <read_first>
    - .planning/phases/01-infrastructure-deploy/01-CONTEXT.md (D-11 and D-12 — the authoritative twelve-item list and the live-cutover framing)
    - .planning/phases/01-infrastructure-deploy/01-RESEARCH.md (Runtime State Inventory — confirms the legacy site has no database, no secrets, and no shared build artifacts, so the move needs no migration)
  </read_first>
  <files>legacy/index.html, legacy/randevu.mp3, legacy/Gruntfile.js, legacy/package.json, legacy/yarn.lock, legacy/scss, legacy/js, legacy/css, legacy/fonts, legacy/img, legacy/json, legacy/files</files>
  <action>
    Create a `legacy/` directory at the repo root and move all twelve artifacts D-12 names into it using `git mv` — never `mv` plus `git add`, because only `git mv` keeps `git log --follow` working across the rename.

    The twelve, by exact name: `index.html`, `randevu.mp3`, `Gruntfile.js`, `package.json`, `yarn.lock`, and the directories `scss/`, `js/`, `css/`, `fonts/`, `img/`, `json/`, `files/`. Note the count — RESEARCH.md's prose says ten, but D-12's enumeration is twelve and is authoritative. Move each one explicitly by name rather than globbing, so nothing is caught or missed by accident.

    Leave everything else at the root untouched: `CLAUDE.md`, `.planning/`, `.git/`, `website/`, and any untracked scratch file such as `loopcaption.md` are NOT part of D-12's list and stay where they are.

    Add `legacy/README.md` — three or four lines stating that this tree is the archived 2019 DARLNG placeholder, that it is retained for reference only, that it is outside the Docker build context so it is never served, and that deleting it is deferred to milestone cleanup by explicit decision.

    Do not modify the contents of any moved file. The 2019 site is an archive, not something to tidy.
  </action>
  <acceptance_criteria>
    - `test -d legacy` and each of these exists: `legacy/index.html`, `legacy/randevu.mp3`, `legacy/Gruntfile.js`, `legacy/package.json`, `legacy/yarn.lock`, `legacy/scss`, `legacy/js`, `legacy/css`, `legacy/fonts`, `legacy/img`, `legacy/json`, `legacy/files`
    - None of the twelve remain at the root: `test ! -e index.html && test ! -e randevu.mp3 && test ! -e Gruntfile.js && test ! -e package.json && test ! -e yarn.lock && test ! -e scss && test ! -e js && test ! -e css && test ! -e fonts && test ! -e img && test ! -e json && test ! -e files`
    - `git status --porcelain` shows the twelve as renames (`R` status), not as deletions plus additions
    - `git log --follow --oneline -- legacy/index.html | wc -l` is greater than 1 (2019 history is reachable through the rename)
    - `test -f legacy/README.md`
    - Plan 01's build is unaffected: `npm run build --prefix website` still exits 0
    - `test -f CLAUDE.md && test -d .planning && test -d website` (nothing outside D-12's list was moved)
  </acceptance_criteria>
  <verify>
    <automated>test -d legacy && test -f legacy/index.html && test -f legacy/Gruntfile.js && test -f legacy/yarn.lock && test -d legacy/scss && test -d legacy/files && test ! -e index.html && test ! -e Gruntfile.js && test ! -e package.json && test -f legacy/README.md && test -f CLAUDE.md && npm run build --prefix website</automated>
  </verify>
  <done>All twelve D-12 artifacts are under `legacy/` as git renames with history intact, the repo root is reduced to `CLAUDE.md`, `legacy/`, `website/` and `.planning/`, and the Plan 01 build still succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Author website/DEPLOY.md — the complete user-executed live runbook</name>
  <reversibility rating="reversible">A documentation file; rewriting it costs nothing and nothing depends on its internal wording except the four section headings named in the artifact contract.</reversibility>
  <read_first>
    - .planning/phases/01-infrastructure-deploy/01-CONTEXT.md (D-01 through D-15 — every live step traces to one of these)
    - .planning/phases/01-infrastructure-deploy/01-RESEARCH.md (the DEPLOY.md skeleton in Code Examples; the Don't Hand-Roll table for Coolify/Resend/Listmonk native mechanisms; Common Pitfalls 3 and 4; the Runtime State Inventory row about the old application's domain binding; Assumptions A2 and A3)
    - website/Dockerfile and website/nginx.conf (the artifacts Coolify will build — the runbook must describe them accurately)
    - .planning/phases/01-infrastructure-deploy/01-01-SUMMARY.md (what Plan 01 actually verified, including whether the container path ran or fell back)
    - .planning/ROADMAP.md (success criteria 2 through 5 become the runbook's verification checklist)
  </read_first>
  <files>website/DEPLOY.md</files>
  <action>
    Write the runbook the user follows to take darlng.com live. Audience is the user acting as operator, not an engineer mid-design: every step is a numbered, tickable checkbox naming the exact screen, field, and value. Prefer Coolify's UI-driven native paths over CLI or YAML alternatives throughout — the user is executing this by hand during a real cutover.

    Open with a short header stating that this file is executed by the user after the local build is approved, that nothing in it is automated, and a prominent WARNING: the legacy files have been archived into `legacy/`, so the currently-live 2019 placeholder will stop building from the repo root on the next push to `master`. The cutover must be completed rather than left half-done (D-11, D-15).

    `## 1. Coolify — Static Site Application` (D-09, D-10, D-13, D-14, D-15): point a Coolify application at the existing GitHub remote on branch `master`; set Base Directory to `website/`; set Build Pack to Dockerfile; add both `https://darlng.com` and `https://www.darlng.com` to Domains; under Advanced set the redirect Direction to www->apex and enable Force HTTPS; deploy and confirm Let's Encrypt issues over HTTP-01, which will succeed because the A record at LWS already resolves to this box. Include the collision step explicitly and early: if an existing Coolify application already binds darlng.com, remove or reassign that binding FIRST — two applications cannot both claim the same domain, and this is the most likely way the cutover stalls. State the choice plainly: either repoint the existing application's Base Directory and Build Pack, or create a new application and move the domain binding, but not both. Record the built-in Direction setting as the primary path and hand-written Traefik redirect labels as a named fallback only if the installed Coolify version lacks that UI control. Note that TLS terminates at Traefik and the container is HTTP-only on port 80 internally, so no certificate work happens inside the image.

    `## 2. Listmonk + Postgres` (D-01, D-02, D-06): deploy through Coolify's Services -> New Resource -> Listmonk one-click picker, NOT by importing a hand-modified compose file — a known Coolify issue breaks the template when the compose source is a private fork, and the one-click path provisions Postgres and wires the connection variables automatically. Set the subdomain to `mail.darlng.com`. Then, as the first action after the service is reachable and before any other configuration, change the default admin password. Then configure SMTP (see section 3's ordering note), then create the fan list with double opt-in enabled, then copy the list UUID and record it for Phase 4. Say explicitly that the UUID and the Listmonk URL are pasted into Coolify's environment-variable UI for the static site application as `PUBLIC_LISTMONK_LIST_UUID` and `PUBLIC_LISTMONK_URL`, that they are build-time values so the site must be redeployed after setting them, and that neither belongs in a file in this repository.

    `## 3. Resend — Domain & DNS` (D-03, D-04, D-05): explain in one line why there is no mail server to install — Listmonk is an SMTP client, not an MTA, so it authenticates to Resend on submission port 587 and Hetzner's port-25 block is irrelevant. Record the SMTP settings Listmonk needs: host `smtp.resend.com`, port `587`, STARTTLS, auth protocol `login`, username `resend`, password = a Resend API key. Add an explicit instruction to cross-check these against Resend's own SMTP documentation before saving, because they are account-scoped and this document's copy is second-hand. Then: add the domain in the Resend dashboard following Resend's own subdomain recommendation for DKIM alignment, open the Records tab, and copy each record verbatim into LWS DNS management. State clearly that the DKIM public key and selector are unique to this account and must be copied from that dashboard — do not write any example key value into this file, because a plausible-looking fake produces records that silently never verify. Finish with waiting for Resend to show all records verified and confirming SPF, DKIM and DMARC with an external checker such as mxtoolbox before trusting deliverability.

    Ordering matters and must be stated in the document: Resend's domain must be verified before Listmonk's SMTP settings are worth testing, and the fan list must exist before its UUID can be recorded. Cross-reference between sections 2 and 3 so a reader working top-to-bottom is not sent backwards.

    `## 4. Post-Cutover Verification`: a copy-pasteable checklist that is exactly roadmap success criteria 2 through 5, expressed as commands — `curl -I https://darlng.com` returns 200 and serves the new build rather than the old jQuery markup; `curl -I https://www.darlng.com` returns 301 to the apex with no loop; `curl -I http://darlng.com` returns 301 to HTTPS; `curl -I https://darlng.com/index.html` shows `Cache-Control: no-cache`; a hashed `/_astro/` asset shows `Cache-Control: public, max-age=31536000, immutable`; a nonexistent path returns 404; the Listmonk admin is reachable at `mail.darlng.com` on a changed password with a double-opt-in fan list present; a test subscription delivers a confirmation email that does not land in spam. Note which of these Plan 01 already proved locally against the container so the user knows which are genuinely new risk at cutover and which are re-confirmations.

    Throughout: never write a real credential value, and never invent an account-specific value. Every credential is described as where to get it and where to paste it.
  </action>
  <acceptance_criteria>
    - `test -f website/DEPLOY.md` and it is at least 90 lines
    - All four section headings are present: `grep -q '^## 1\. Coolify' website/DEPLOY.md`, `grep -q '^## 2\. Listmonk' website/DEPLOY.md`, `grep -q '^## 3\. Resend' website/DEPLOY.md`, `grep -q '^## 4\. Post-Cutover Verification' website/DEPLOY.md`
    - Locked values appear verbatim: `grep -q 'mail.darlng.com'`, `grep -q 'smtp.resend.com'`, `grep -q '587'`, `grep -q 'Base Directory'`, `grep -q 'website/'`, `grep -q 'Dockerfile'`, `grep -q 'double opt-in'` (case-insensitive), `grep -q 'PUBLIC_LISTMONK_LIST_UUID'`, `grep -q 'PUBLIC_LISTMONK_URL'` — each against `website/DEPLOY.md`
    - The domain-collision step is present: `grep -qi 'domain binding' website/DEPLOY.md`
    - The legacy-cutover warning is present: `grep -qi 'legacy' website/DEPLOY.md` and `grep -qi 'master' website/DEPLOY.md`
    - The redirect requirements are present: `grep -qi 'force https' website/DEPLOY.md` and `grep -qi 'direction' website/DEPLOY.md`
    - No secret-shaped literal is committed: `! grep -Eq 're_[A-Za-z0-9]{16,}' website/DEPLOY.md` and `! grep -Eiq '(password|api[_ -]?key)[[:space:]]*[=:][[:space:]]*[A-Za-z0-9]{12,}' website/DEPLOY.md`
    - The verification checklist covers all four cache/redirect/404 checks: `grep -c 'curl -I' website/DEPLOY.md` returns at least 5
    - `grep -qi 'mxtoolbox' website/DEPLOY.md` (D-05 external verification step is present)
    - DEPLOY.md is excluded from the Docker build context: `grep -q 'DEPLOY.md' website/.dockerignore`
  </acceptance_criteria>
  <verify>
    <automated>test -f website/DEPLOY.md && test "$(wc -l < website/DEPLOY.md)" -ge 90 && grep -q '^## 1\. Coolify' website/DEPLOY.md && grep -q '^## 2\. Listmonk' website/DEPLOY.md && grep -q '^## 3\. Resend' website/DEPLOY.md && grep -q '^## 4\. Post-Cutover Verification' website/DEPLOY.md && grep -q 'mail.darlng.com' website/DEPLOY.md && grep -q 'smtp.resend.com' website/DEPLOY.md && grep -q 'PUBLIC_LISTMONK_LIST_UUID' website/DEPLOY.md && grep -qi 'domain binding' website/DEPLOY.md && ! grep -Eq 're_[A-Za-z0-9]{16,}' website/DEPLOY.md && test "$(grep -c 'curl -I' website/DEPLOY.md)" -ge 5</automated>
  </verify>
  <done>`website/DEPLOY.md` documents every live step for INFRA-02, INFRA-03 and INFRA-04 with concrete screens, fields and values; contains no credential and no invented account-specific value; and ends with a verification checklist matching roadmap success criteria 2 through 5.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo (public GitHub) -> anyone | `DEPLOY.md` is committed to a world-readable repo; it describes credentials and is the single most likely place for one to be pasted |
| Runbook -> user's live infrastructure | Instructions authored here are executed by hand against production DNS, TLS, and a mail-sending identity; a wrong or invented value causes a silent failure the user discovers late |
| Listmonk -> Resend (deferred to live) | Authenticated SMTP submission on port 587 carries the sending identity for darlng.com |
| Public internet -> Listmonk admin (deferred to live) | The admin panel is internet-reachable at `mail.darlng.com` once deployed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-09 | Information Disclosure | `website/DEPLOY.md` in a public repo | high | mitigate | Task 2 authors every credential as "where to obtain it, where to paste it", never as a value; all secrets are entered in Coolify's environment-variable UI or Listmonk's settings screen. Enforced by two negative-grep acceptance criteria for API-key-shaped and password-assignment-shaped literals, and carried as an explicit prohibition in `must_haves.prohibitions`. |
| T-01-10 | Elevation of Privilege | Listmonk admin panel | high | mitigate | Task 2 places "change the default admin password" as the first action after the service is reachable and before any other configuration step, so an operator following the runbook top-to-bottom cannot leave defaults in place while the panel is internet-exposed (D-06). Execution is the user's, but the ordering that prevents the window is delivered here. |
| T-01-11 | Spoofing | darlng.com sending identity | high | mitigate | Task 2 requires SPF, DKIM and DMARC to be configured from Resend's account-specific Records tab and externally confirmed before deliverability is trusted (D-05). It explicitly forbids hand-typed generic record values, which is the failure mode that produces records that look right and never verify. Transport security is Resend's responsibility (D-03/D-04 rejected a self-hosted MTA precisely to avoid owning sending reputation). |
| T-01-12 | Tampering | Coolify auto-deploy from `master` | medium | mitigate | The runbook documents Base Directory `website/` as mandatory (D-09), which confines the build context so that neither the archived `legacy/` tree nor repo-root files can enter a deployed image even though CD is push-triggered (D-10). |
| T-01-13 | Denial of Service | Live domain during cutover | medium | accept | D-15 chose a direct flip; because DNS already resolves to the Coolify box, the exposure is the brief window while the new application takes the domain from the old one. Accepted as a deliberate decision, with the runbook reducing it by making the binding-collision step explicit and early. |
| T-01-14 | Repudiation | Archived `legacy/` tree | low | accept | The 2019 assets remain in the repo and in git history by decision (D-12 archives rather than deletes). They are outside the Docker build context and unreachable at runtime. Accepted; deletion revisited at milestone cleanup. |

No threat is dispositioned `accept` at severity high or above, so the configured blocking threshold (`high`) is clear.
</threat_model>

<verification>
1. `test -d legacy && test ! -e index.html && test ! -e Gruntfile.js` — the archive move is complete
2. `git status --porcelain | grep -c '^R'` shows the twelve artifacts as renames
3. `git log --follow --oneline -- legacy/index.html` reaches 2019 commits
4. `npm run build --prefix website` still exits 0 — the move did not disturb the site
5. `test "$(wc -l < website/DEPLOY.md)" -ge 90` and all four `## N.` headings are present
6. `! grep -Eq 're_[A-Za-z0-9]{16,}' website/DEPLOY.md` — no API-key-shaped literal committed
7. A human read-through of DEPLOY.md sections 1-4 confirms each step names a concrete screen, field and value rather than a general intention

Live verification of INFRA-02's deploy, INFRA-03's redirects and INFRA-04's Listmonk instance is DEFERRED to the user per the scope revision and is captured as DEPLOY.md section 4 rather than as acceptance criteria here.
</verification>

<success_criteria>
- `website/` is unambiguously the site: the repo root holds only `CLAUDE.md`, `legacy/`, `website/` and `.planning/`
- The 2019 placeholder is archived with git history intact and is unreachable from any build context
- The user can execute the entire live cutover — Coolify application, domain, TLS, redirects, Listmonk, Postgres, Resend, LWS DNS — from `website/DEPLOY.md` alone, without returning for engineering input
- No credential and no invented account-specific value is committed to the repository
- Phase 4's two environment variables have a documented origin and a documented destination
</success_criteria>

<output>
Create `.planning/phases/01-infrastructure-deploy/01-02-SUMMARY.md` when done. Record: the twelve moved paths, confirmation that git recorded them as renames, the DEPLOY.md section list, and any point where RESEARCH.md's second-hand values (Resend SMTP settings, Coolify UI labels) were flagged in the runbook for the user to re-confirm at execution time.
</output>
