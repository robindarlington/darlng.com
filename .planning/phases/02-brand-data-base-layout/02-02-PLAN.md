---
phase: 02-brand-data-base-layout
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - website/src/assets/releases/randevu.jpg
  - website/src/assets/releases/brave.jpg
  - website/src/assets/releases/open-wide.jpg
  - website/src/data/releases.ts
  - website/src/components/BrandIcon.astro
  - website/src/components/Header.astro
  - website/src/components/Footer.astro
  - website/src/styles/global.css
  - website/src/pages/404.astro
autonomous: true
requirements: [BRAND-03, BRAND-04]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A fan can reach any of the five DARLNG profiles from both the header and the footer at every tested viewport, and each anchor opens the correct profile URL in a new tab."
    - "Header and Footer each render exactly one anchor per entry in `socials` by iterating the array rather than hardcoding markup — `dist/index.html` contains exactly 10 occurrences of the follow aria-label prefix, which is 2 x `socials.length`."
    - "Social anchors render in CONTENT.md order — Spotify, Instagram, Facebook, YouTube, TikTok — in both header and footer; `releases` is ordered newest-first (eseriani, randevu, brave, open-wide) and exactly one entry carries `isLatest: true`, so latest-release selection never depends on array position."
    - "Adjacent social tap targets never collide: at 375px every social anchor measures at least 44x44 CSS px and no two adjacent anchors' bounding rects intersect, with at least 8px between neighbours in the same row."
    - "`src/data/releases.ts` exports all four releases with 22 platform links in total (eseriani 4, randevu 7, brave 5, open-wide 6), every URL transcribed from .planning/CONTENT.md, and every cover imported as `ImageMetadata`."
    - "The layout is single-column at 375px and multi-column from 768px up (three-track footer grid), with no horizontal overflow at 375px, 768px or 1440px."
    - "`/404` renders inside the same `Layout` with the same header, footer and tokens, and `dist/404.html` is still emitted so the Phase 1 nginx real-404 chain keeps working."
    - "Every external anchor that opens in a new tab carries `rel=\"noopener noreferrer\"` — the counts of `target=\"_blank\"` and of the rel attribute are equal in both `dist/index.html` and `dist/404.html`."
  artifacts:
    - path: "website/src/components/BrandIcon.astro"
      provides: "Zero-JS inline brand SVG renderer taking a simple-icons icon object and inheriting colour from CSS"
      contains: "currentColor"
    - path: "website/src/data/releases.ts"
      provides: "Complete four-release catalog with all platform links and the social profile list"
      exports: ["releases", "socials"]
    - path: "website/src/assets/releases/randevu.jpg"
      provides: "Randevu cover-art source (2450x2450)"
    - path: "website/src/assets/releases/brave.jpg"
      provides: "Brave cover-art source (3000x3000)"
    - path: "website/src/assets/releases/open-wide.jpg"
      provides: "Open Wide cover-art source (3000x3000)"
    - path: "website/src/components/Header.astro"
      provides: "Wordmark plus the five-platform social-follow nav with 44px tap targets"
    - path: "website/src/components/Footer.astro"
      provides: "Three-column footer with wordmark, genre line, social row and copyright"
    - path: "website/src/pages/404.astro"
      provides: "Layout-wrapped 404 page proving the layout is reusable across routes"
  key_links:
    - from: "website/src/components/Header.astro"
      to: "website/src/data/releases.ts"
      via: "imports socials and maps one anchor per entry"
      pattern: "socials"
    - from: "website/src/components/Footer.astro"
      to: "website/src/data/releases.ts"
      via: "imports socials and maps one anchor per entry"
      pattern: "socials"
    - from: "website/src/components/Header.astro"
      to: "website/src/components/BrandIcon.astro"
      via: "renders BrandIcon per social entry with the mapped simple-icons object"
      pattern: "BrandIcon"
    - from: "website/src/components/BrandIcon.astro"
      to: "simple-icons"
      via: "SimpleIcon type import and path field rendered into an inline svg path element"
      pattern: "simple-icons"
    - from: "website/src/pages/404.astro"
      to: "website/src/layouts/Layout.astro"
      via: "wraps page content in the shared Layout component"
      pattern: "layouts/Layout\\.astro"
  prohibitions:
    - requirement_id: BRAND-04
      category: accessibility
      statement: "MUST NOT remove, suppress or render invisible the keyboard focus indicator on any interactive element — every link and icon anchor keeps the specified focus-visible outline in the accent colour; a focus style is never traded away for visual cleanliness."
    - requirement_id: BRAND-04
      category: accuracy
      statement: "MUST NOT alter, abbreviate or invent release titles, years, featured-artist credits, or the host and path of any streaming or social URL — every value is transcribed from .planning/CONTENT.md. The single permitted normalisation is upgrading the one Open Wide Amazon URL from an insecure to a secure scheme, same host and same path, recorded as T-02-02-04 in this plan's threat model."
---

<objective>
Complete the DARLNG data foundation and base layout: the full four-release catalog with every platform link, the zero-JS brand-icon component, the five-platform social-follow rows in header and footer, the 404 page rewrapped in the shared layout, and a real browser pass at 375px, 768px and 1440px that resolves the UI-SPEC's one open layout question.

Purpose: Phase 3's hero, discography and listen pages read every field this plan writes, and the social-follow anchors are the FAN-03 groundwork the header and footer must carry from now on. The browser pass is the acceptance gate the CONTENT.md scope revision requires in place of live-domain verification.

Output: three cover-art assets, three additional `Release` entries, `BrandIcon.astro`, social rows in `Header.astro` and `Footer.astro`, a Layout-wrapped `404.astro`, and verified responsive behaviour at all three test viewports.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/CONTENT.md
@.planning/phases/02-brand-data-base-layout/02-CONTEXT.md
@.planning/phases/02-brand-data-base-layout/02-UI-SPEC.md
@.planning/phases/02-brand-data-base-layout/02-RESEARCH.md
@.planning/phases/02-brand-data-base-layout/02-01-SUMMARY.md
@website/src/data/releases.ts
@website/src/components/Header.astro
@website/src/components/Footer.astro
@website/src/pages/404.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expansion — the full four-release catalog with every platform link</name>
  <precondition>`website/src/data/releases.ts` from plan 02-01 exists and exports the `Release`, `PlatformLink` and `Platform` types with the Eseriani entry.</precondition>
  <reversibility rating="reversible">Additive data rows behind the `Release` contract that plan 02-01 already fixed; no consumer signature changes.</reversibility>
  <read_first>
    website/src/data/releases.ts,
    .planning/CONTENT.md (Randevu, Brave and Open Wide release blocks — the authoritative URL list),
    .planning/phases/02-brand-data-base-layout/02-RESEARCH.md (Pattern 3, Pitfall 2, Open Questions item 1),
    .planning/phases/02-brand-data-base-layout/02-CONTEXT.md (Data Model)
  </read_first>
  <files>website/src/assets/releases/randevu.jpg, website/src/assets/releases/brave.jpg, website/src/assets/releases/open-wide.jpg, website/src/data/releases.ts</files>
  <action>
    Copy the three remaining cover-art sources into `website/src/assets/releases/`: `/Users/rob/Desktop/DARLNG/RANDEVU/RANDEVU.jpg` becomes `randevu.jpg`, `/Users/rob/Desktop/DARLNG/BRAVE/BRAVE.jpg` becomes `brave.jpg`, and `/Users/rob/Desktop/DARLNG/OPEN WIDE/OPEN_WIDE.jpg` (note the space in the source directory name — quote the path) becomes `open-wide.jpg`. They go under `src/assets/`, never `public/`.

    Add three top-level imports to `website/src/data/releases.ts` alongside the existing Eseriani import, then append three `Release` entries to the exported `releases` array, keeping the array in newest-first order after Eseriani. The `Platform` union defined in plan 02-01 already covers all eleven platforms used here; do not add members to it.

    Randevu — slug `randevu`, title `Randevu`, year `2024`, artistLine `Darlng ft. Shubi Di Badman`, cover the imported randevu asset, `isLatest: false`, and seven platform links transcribed from CONTENT.md's Randevu block in that block's listed order: spotify, deezer, amazonMusic, tidal, pandora, napster, anghami — with display labels `Spotify`, `Deezer`, `Amazon Music`, `Tidal`, `Pandora`, `Napster`, `Anghami`. Randevu is not on Apple Music; do not invent an entry for it. No `youtubeEmbed`.

    Brave — slug `brave`, title `Brave`, year `2020`, artistLine `Darlng ft. Ray Pineapple`, cover the imported brave asset, `isLatest: false`, and five platform links in CONTENT.md's listed order: spotify, appleMusic, tidal, pandora, boomplay — labels `Spotify`, `Apple Music`, `Tidal`, `Pandora`, `Boomplay`. No `youtubeEmbed`.

    Open Wide — slug `open-wide`, title `Open Wide`, year `2019`, artistLine `Darlng ft. Don Classic`, cover the imported open-wide asset, `isLatest: false`, and six platform links in CONTENT.md's listed order: spotify, appleMusic, amazonMusic, deezer, iheartradio, napster — labels `Spotify`, `Apple Music`, `Amazon Music`, `Deezer`, `iHeartRadio`, `Napster`. CONTENT.md explicitly instructs skipping the dead Google-Play-backed music link for this release; do not include it. CONTENT.md records the Amazon URL for this release with an insecure scheme — write it with the secure `https://` scheme, exact same host and path (`www.amazon.com/gp/product/B082RBHFWH`). That scheme upgrade is the only permitted normalisation of any URL in this file; it is recorded as `T-02-02-04` in this plan's threat model, and it changes no destination. No `youtubeEmbed`.

    Exclude the entries CONTENT.md marks "(reference only)" at the bottom of each release block — those are aggregator landing pages, not platform destinations, and none of them belongs in the shipped catalog. Every other character of every URL is transcribed exactly as recorded. Leave the `socials` export untouched; plan 02-01 already authored it.
  </action>
  <verify>
    <automated>cd website && npm run check && npm run build && test "$(grep -c 'isLatest:' src/data/releases.ts)" = "4" && test "$(grep -c 'isLatest: true' src/data/releases.ts)" = "1" && test "$(grep -c "slug: '" src/data/releases.ts)" = "4" && test "$(grep -c "platform: '" src/data/releases.ts)" = "27" && grep -q 'play.anghami.com/song/1166313324' src/data/releases.ts && grep -q 'boomplay.com/songs/144156765' src/data/releases.ts && grep -q 'us.napster.com/artist/darlng/album/open-wide' src/data/releases.ts && grep -q 'iheart.com/artist/id-51034697' src/data/releases.ts && ! grep -q 'hyperfollow' src/data/releases.ts && ! grep -q 'song.link' src/data/releases.ts && ! grep -q "url: 'http://" src/data/releases.ts</automated>
  </verify>
  <acceptance_criteria>
    - `website/src/assets/releases/` contains exactly `eseriani.jpg`, `randevu.jpg`, `brave.jpg`, `open-wide.jpg`.
    - `sips -g pixelWidth website/src/assets/releases/randevu.jpg` reports 2450; `brave.jpg` and `open-wide.jpg` each report 3000.
    - `grep -c "slug: '" website/src/data/releases.ts` outputs `4`, and the slugs appear in file order `eseriani`, `randevu`, `brave`, `open-wide`.
    - `grep -c "isLatest: true" website/src/data/releases.ts` outputs `1` and `grep -c "isLatest:" website/src/data/releases.ts` outputs `4`.
    - `grep -c "platform: '" website/src/data/releases.ts` outputs `27` — 22 release platform links (eseriani 4, randevu 7, brave 5, open-wide 6) plus the 5 `socials` entries.
    - `grep -c "cover: " website/src/data/releases.ts` outputs `4`.
    - `grep -c "url: 'http://" website/src/data/releases.ts` outputs `0` — every shipped URL uses the secure scheme.
    - `grep -c 'hyperfollow' website/src/data/releases.ts` outputs `0` and `grep -c 'song.link' website/src/data/releases.ts` outputs `0`.
    - `grep -c 'music.apple.com' website/src/data/releases.ts` outputs `3` — Eseriani, Brave and Open Wide only; Randevu has no Apple Music entry.
    - `cd website && npm run check` exits 0 with 0 errors, proving all four `cover` fields type as `ImageMetadata`.
    - `cd website && npm run build` exits 0.
  </acceptance_criteria>
  <done>`src/data/releases.ts` is the complete single source of truth for the DARLNG catalog: four releases, 22 platform links, four `ImageMetadata` covers, five social profiles — all type-checked and building clean.</done>
</task>

<task type="auto">
  <name>Task 2: Expansion — a fan can follow DARLNG on all five platforms from the header and the footer</name>
  <precondition>`website/src/data/releases.ts` exports `socials` with five entries and `simple-icons@^16.28.0` is installed in `website/node_modules` (both from plan 02-01).</precondition>
  <reversibility rating="reversible">A new leaf component plus additive markup in two existing components; removal restores the plan 02-01 header and footer exactly.</reversibility>
  <read_first>
    .planning/phases/02-brand-data-base-layout/02-UI-SPEC.md (Header contract, Footer contract, Icon implementation, Interaction states, Spacing Scale exception, Copywriting Contract),
    .planning/phases/02-brand-data-base-layout/02-RESEARCH.md (Code Examples — BrandIcon.astro and its usage; Pitfall 3 — simple-icons v16 named-export shape; Anti-Patterns),
    website/src/components/Header.astro, website/src/components/Footer.astro, website/src/pages/404.astro, website/src/styles/global.css, website/src/data/releases.ts,
    .planning/CONTENT.md (Social Profiles)
  </read_first>
  <files>website/src/components/BrandIcon.astro, website/src/components/Header.astro, website/src/components/Footer.astro, website/src/styles/global.css, website/src/pages/404.astro</files>
  <action>
    STEP 1 — `website/src/components/BrandIcon.astro`. Frontmatter declares `import type { SimpleIcon } from 'simple-icons';` and a `Props` interface of `{ icon: SimpleIcon; class?: string }`, destructuring `class` to `className`. The template renders a single `svg` element with `viewBox="0 0 24 24"`, `fill="currentColor"`, `role="img"`, `aria-hidden="true"`, the passed class, and one `path` element whose `d` is bound to the icon's path field. The colour must come from CSS through `currentColor` only — never read the icon object's brand-colour field, because five clashing platform brand colours would break the single-jewel-accent architecture the UI-SPEC mandates. This component ships zero JavaScript.

    STEP 2 — the icon lookup. In both `Header.astro` and `Footer.astro` frontmatter, import the five brand marks by their v16 named exports — `import { siSpotify, siInstagram, siFacebook, siYoutube, siTiktok } from 'simple-icons';` — and build a module-local record keyed by the `SocialLink['platform']` union mapping to those objects. Do not use any sub-path import style; RESEARCH.md Pitfall 3 documents that older shapes no longer resolve in v16.

    STEP 3 — the shared anchor treatment. Add a `.social-icon-link` rule to `website/src/styles/global.css` implementing the UI-SPEC hover glow that Tailwind utilities express poorly: on hover, `box-shadow: 0 0 12px color-mix(in oklch, var(--color-accent) 40%, transparent);`. Everything else stays in utility classes on the anchor.

    STEP 4 — `Header.astro`. Add a `nav` element with `aria-label="Follow DARLNG"` as the right-hand child of the existing container flex row, classed `flex items-center gap-2 md:gap-4`. Map over `socials` imported from `../data/releases`, emitting one anchor per entry with `href` bound to the entry URL, `target="_blank"`, `rel="noopener noreferrer"`, `aria-label` of `Follow DARLNG on ` followed by the entry label, class `social-icon-link flex items-center justify-center min-h-11 min-w-11 rounded-full text-text-muted transition-colors duration-150 hover:text-accent focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-accent active:scale-95`, containing a `BrandIcon` with the mapped icon and class `w-5 h-5`. The row must be produced by iterating `socials`, never by hand-writing five anchors, so the rendered count always equals `socials.length`.

    STEP 5 — `Footer.astro`. Put the identical anchor row into the middle grid cell that plan 02-01 left empty, classed `flex items-center justify-center gap-4`, same 44px minimum targets, same aria-labels, same order. Iterate the same `socials` array.

    STEP 6 — `website/src/pages/404.astro`. Replace the Phase 1 standalone page with a `Layout`-wrapped page, title `Not found — DARLNG`, description `That page does not exist on darlng.com.` Content: a `font-display font-extrabold text-[40px] leading-[1.05]` heading reading `404`, a `text-base text-text-muted max-w-prose` line reading `That page does not exist. The music does.` in the confident, non-corporate artist voice, and a link back to `/` styled per the UI-SPEC secondary/ghost button contract — `inline-flex items-center rounded-full border-[1.5px] border-accent text-accent py-3 px-6 transition-colors hover:bg-accent/10 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-accent` — reading `Back to the music`. Wrap it in a `py-12 md:py-16` section. This page must still compile to `dist/404.html`, which the Phase 1 nginx real-404 chain serves.
  </action>
  <verify>
    <automated>cd website && npm run check && npm run build && test "$(grep -c 'aria-label="Follow DARLNG on' dist/index.html)" = "10" && test "$(grep -c 'aria-label="Follow DARLNG on' dist/404.html)" = "10" && test "$(grep -c 'rel="noopener noreferrer"' dist/index.html)" = "$(grep -c 'target="_blank"' dist/index.html)" && test "$(grep -c 'rel="noopener noreferrer"' dist/404.html)" = "$(grep -c 'target="_blank"' dist/404.html)" && test "$(grep -o 'Follow DARLNG on [A-Za-z]*' dist/index.html | head -5 | tr '\n' ',')" = "Follow DARLNG on Spotify,Follow DARLNG on Instagram,Follow DARLNG on Facebook,Follow DARLNG on YouTube,Follow DARLNG on TikTok," && grep -q 'open.spotify.com/artist/0uXxSPfLr36OuyGDKiBzV3' dist/index.html && grep -q 'tiktok.com/@darlng_music' dist/index.html && grep -q 'Afro / RnB / Pop' dist/404.html && grep -q 'focus-visible' dist/_astro/*.css</automated>
  </verify>
  <acceptance_criteria>
    - `website/src/components/BrandIcon.astro` exists, contains `fill="currentColor"`, `role="img"`, `aria-hidden="true"`, and `grep -c 'icon.hex' website/src/components/BrandIcon.astro` outputs `0`. <!-- planner-discipline-allow: icon.hex -->
    - `dist/index.html` contains exactly 10 occurrences of `aria-label="Follow DARLNG on` — five in the header, five in the footer, equal to 2 x `socials.length`.
    - `dist/404.html` contains exactly 10 occurrences of the same prefix (the same Layout renders on both routes).
    - The first five follow aria-labels in `dist/index.html` document order read Spotify, Instagram, Facebook, YouTube, TikTok — CONTENT.md's order.
    - In both `dist/index.html` and `dist/404.html`, the count of `rel="noopener noreferrer"` equals the count of `target="_blank"`.
    - All five CONTENT.md social URLs appear verbatim in `dist/index.html`: the Spotify artist URL ending `0uXxSPfLr36OuyGDKiBzV3`, `instagram.com/darlngmusic`, `facebook.com/darlng.music`, the YouTube channel URL ending `UC_noHUKnp2wJ3fi29smmZKw`, and `tiktok.com/@darlng_music`.
    - `website/src/components/Header.astro` and `website/src/components/Footer.astro` each contain a `socials.map` iteration and no hand-written duplicate anchor blocks.
    - `dist/_astro/*.css` contains a `focus-visible` rule and the `social-icon-link` hover rule with `color-mix`.
    - `dist/404.html` exists, contains the heading `404`, the string `Back to the music`, and the footer genre line `Afro / RnB / Pop`.
    - `cd website && npm run check` exits 0 and `cd website && npm run build` exits 0.
  </acceptance_criteria>
  <done>Header and footer each expose all five DARLNG profiles as accessible, keyboard-focusable, 44px icon links in CONTENT.md order, and the 404 page renders inside the same shared Layout.</done>
</task>

<task type="auto">
  <name>Task 3: Expansion — browser-verify the complete layout at 375px, 768px and 1440px</name>
  <reversibility rating="reversible">Verification pass; any fix is a local class change in the header, footer or stylesheet.</reversibility>
  <read_first>
    .planning/CONTENT.md (Scope Revision — build then browser-test at 375/768/1440 workflow),
    .planning/phases/02-brand-data-base-layout/02-UI-SPEC.md (Layout and Responsive Contract, Header contract including the 375px fit question and its wrap fallback, Footer contract, Spacing Scale 44px exception, Interaction states),
    website/src/components/Header.astro, website/src/components/Footer.astro, website/src/styles/global.css
  </read_first>
  <files>website/src/components/Header.astro, website/src/components/Footer.astro, website/src/styles/global.css (fix-on-failure only)</files>
  <action>
    Run `npm run build`, then start the local preview server on port 4321 from `website/` in the background. Drive it with `mcp__t3-code__preview_open` on `http://localhost:4321/`, then `mcp__t3-code__preview_resize` plus `mcp__t3-code__preview_snapshot` at 375x812, 768x1024 and 1440x900, and finally `mcp__t3-code__preview_navigate` to `http://localhost:4321/404` with a snapshot at 375x812.

    This pass resolves the one question the UI-SPEC left open: whether the wordmark plus five 44x44 tap targets with 8px gaps actually fit on one header row at 375px. Measure it rather than eyeballing it. If `document.documentElement.scrollWidth` exceeds `clientWidth`, or any two adjacent social anchors' bounding rects intersect, apply the UI-SPEC's documented fallback — let the icon row wrap to a second row below the wordmark using `flex-wrap` with `justify-center`, keeping the same gaps and the same 44px targets. Never hide an icon, never drop one, and never shrink a target below 44x44; the UI-SPEC forbids all three. Record in the SUMMARY which branch was taken.

    Measure every geometry assertion against `document.documentElement.clientWidth` rather than the nominal viewport width so a visible scrollbar cannot produce a false failure. Fix any failure at its source, re-run `npm run build`, and re-drive the browser until every criterion holds. Stop the preview server when done.
  </action>
  <verify>
    <automated>cd website && npm run build && npm run check && test -f dist/404.html && test "$(grep -c 'aria-label="Follow DARLNG on' dist/index.html)" = "10" && grep -q 'Afro / RnB / Pop' dist/index.html && grep -q 'min-h-11' dist/index.html && ls dist/_astro/ | grep -qE '\.avif$'</automated>
  </verify>
  <acceptance_criteria>
    - Snapshots captured via `mcp__t3-code__preview_snapshot` at 375x812, 768x1024 and 1440x900 on `/`, plus 375x812 on `/404`.
    - At every viewport, `document.documentElement.scrollWidth` is at most `document.documentElement.clientWidth + 1` — no horizontal overflow anywhere.
    - At 375x812, every one of the 10 social anchors reports `getBoundingClientRect()` width of at least 44 and height of at least 44.
    - At 375x812, no two adjacent social anchors' bounding rects intersect, and neighbours sharing a row are separated by at least 8px.
    - At 375x812, the footer's direct children stack vertically: each block's `getBoundingClientRect().top` is at or below the previous block's `bottom`.
    - At 768x1024, `getComputedStyle(footerGrid).gridTemplateColumns` resolves to three track values (multi-column desktop layout, BRAND-03).
    - At 1440x900, the `main` element's `getBoundingClientRect().width` equals `1280` and `Math.abs(rect.left - (document.documentElement.clientWidth - rect.width) / 2)` is at most `1`.
    - `document.fonts.check('600 14px "Manrope Variable"')` returns `true` at every viewport.
    - `dist/_astro/*.css` contains a `:focus-visible` rule declaring a 2px outline in the accent colour, and every social anchor carries the focus-visible utility classes.
    - The `/404` snapshot at 375x812 shows the same header wordmark, the same social row and the same footer as `/`.
  </acceptance_criteria>
  <done>The complete DARLNG base layout is confirmed in a real browser at 375px, 768px and 1440px: no horizontal overflow, at-least-44px social tap targets that never collide, a stacked mobile footer, a three-column desktop footer, and a 404 page matching the site chrome.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build output → fan's browser | Rendered anchors send the fan to third-party origins under those origins' control |
| `simple-icons` package → shipped DOM | Third-party SVG path data is inlined directly into every page's markup |
| .planning/CONTENT.md → shipped anchors | Streaming and social destinations are transcribed into `releases.ts` and rendered as live links |
| local disk (~/Desktop) → repository | Three more cover-art binaries are copied in and committed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-02-01 | Tampering | External social anchors opening in a new tab (reverse tabnabbing) | medium | mitigate | Every anchor emitted by the header and footer social rows carries `rel="noopener noreferrer"` alongside `target="_blank"`, so the opened origin cannot reach back through `window.opener`. Enforced by a count-equality assertion over `dist/index.html` and `dist/404.html` in Task 2's verify command, not by inspection. |
| T-02-02-02 | Spoofing | Social profile URLs rendered as follow links | medium | mitigate | All five URLs are transcribed from the reviewed `.planning/CONTENT.md` Social Profiles list and asserted present verbatim in the build output; none is constructed, templated or user-supplied. A data-entry slip that pointed a follow link elsewhere would fail the verbatim assertion. |
| T-02-02-03 | Information Disclosure | `simple-icons` inline SVG path data | low | accept | The package is a data-only export of normalised path strings under CC0-1.0 (registry-confirmed in RESEARCH.md); only the five imported icon objects are bundled, the output is inert inline SVG markup, and no script or remote reference is introduced. License: CC0-1.0, public domain dedication — no attribution obligation, noted here for the record. |
| T-02-02-04 | Tampering | Open Wide Amazon URL recorded with an insecure scheme in CONTENT.md | medium | mitigate | Shipped with the secure scheme, identical host and path. Leaving the plaintext scheme would let a network attacker rewrite the redirect target of a fan's outbound click. Enforced by asserting zero insecure-scheme URLs in `releases.ts`. Recorded as the sole permitted URL normalisation in this plan's data-accuracy prohibition so it is a documented decision, not a silent edit. |
| T-02-02-05 | Denial of Service | Three additional Sharp-processed cover-art sources (up to 3000x3000) | low | accept | Build-time only on four fixed local files; no runtime image processing exists on a static site. Per-image `widths` stay capped below each source resolution (RESEARCH.md Pitfall 2). |
| T-02-02-SC | Tampering | Package-manager installs | low | accept | This plan installs no packages; the four-package supply-chain gate is `T-02-01-SC` in plan 02-01, where the RESEARCH.md Package Legitimacy Audit cleared all four with no `[ASSUMED]`/`[SUS]` residue. `package.json` and `package-lock.json` are untouched here. |
</threat_model>

<flagged_planner_assumptions>
Edge-coverage rows the probe left `unclassified`. Per the spec-less fallback protocol these stay **unresolved** — never auto-resolved, never dropped — and are carried forward as explicit planner assumptions.

**BRAND-03 — unclassified (`unclassified — review manually`).**
Planner assumption: BRAND-03's "single-column on mobile, grid/flex on desktop" is operationalised as exactly the three viewports the CONTENT.md scope revision names — 375x812, 768x1024 and 1440x900 — with the specific rect and computed-style assertions in this plan's Task 3. Viewports outside that set are **not** asserted this phase: narrower than 375px (e.g. 320px), wider than 1440px (e.g. 2560px, where the 1280px container's side gutters grow), landscape phone orientations, and text-zoom or `prefers-reduced-motion` variants. No breakpoint behaviour is claimed for them. **UNRESOLVED, carried forward.**
</flagged_planner_assumptions>

## Artifacts this phase produces

### Created by this plan (02-02)

**Files**
- `website/src/assets/releases/randevu.jpg` (2450x2450), `brave.jpg` (3000x3000), `open-wide.jpg` (3000x3000)
- `website/src/data/releases.ts` (modified) — three additional `Release` entries, 22 platform links total
- `website/src/components/BrandIcon.astro` — inline brand SVG renderer
- `website/src/components/Header.astro` (modified) — five-platform social-follow nav
- `website/src/components/Footer.astro` (modified) — social row in the middle grid column
- `website/src/styles/global.css` (modified) — `.social-icon-link` hover-glow rule
- `website/src/pages/404.astro` (rewritten) — Layout-wrapped 404

**Exported symbols**
- `website/src/data/releases.ts`: `releases` now holds 4 `Release` entries (`eseriani`, `randevu`, `brave`, `open-wide`); `socials` unchanged at 5 `SocialLink` entries
- `website/src/components/BrandIcon.astro`: default Astro component, props `{ icon: SimpleIcon; class?: string }`

**CSS classes added**
- `.social-icon-link` — accent hover glow via `color-mix(in oklch, var(--color-accent) 40%, transparent)`

**Imported third-party symbols**
- `simple-icons`: `siSpotify`, `siInstagram`, `siFacebook`, `siYoutube`, `siTiktok`, type `SimpleIcon` (CC0-1.0)

### Created by plan 02-01 (reference only)

`website/src/styles/global.css` `@theme` tokens (`--color-bg`, `--color-surface`, `--color-text`, `--color-text-muted`, `--color-accent`, `--color-error`, `--font-display`, `--font-body`, `--radius-card`, `--radius-pill`); `website/src/data/releases.ts` type contract (`Platform`, `PlatformLink`, `YouTubeEmbed`, `Release`, `SocialLink`) and the `eseriani` entry plus `socials`; `website/src/layouts/Layout.astro`; `website/src/components/Header.astro` and `Footer.astro` shells; `website/src/pages/index.astro`; `website/src/assets/releases/eseriani.jpg`; `website/scripts/check-contrast.mjs`; npm scripts `check:contrast` and the extended `check`.

<verification>
1. `cd website && npm run check` exits 0 — `astro check` reports 0 errors across all four `ImageMetadata` covers, and `check:contrast` still prints 9 PASS lines.
2. `cd website && npm run build` exits 0 and emits `dist/index.html`, `dist/404.html`, and Sharp-generated avif/webp derivatives under `dist/_astro/`.
3. `dist/index.html` and `dist/404.html` each carry exactly 10 follow anchors, in CONTENT.md order, with matched `target="_blank"` and `rel="noopener noreferrer"` counts.
4. `releases.ts` holds 4 releases and 22 platform links with no aggregator URL and no insecure-scheme URL.
5. Browser snapshots at 375x812, 768x1024 and 1440x900 satisfy every assertion in Task 3's acceptance criteria, including the 44px non-colliding tap targets and the three-track footer grid.
6. `website/package.json` is unmodified by this plan — the Phase 1 Tailwind pins and `overrides.vite` remain intact.
</verification>

<success_criteria>
- Social follow links for Spotify, Instagram, Facebook, YouTube and TikTok are present in both header and footer, keyboard-focusable, at least 44x44 px, and open the correct profiles (BRAND-04).
- The layout is single-column at 375px and multi-column from 768px, with no horizontal overflow at any tested viewport (BRAND-03).
- `src/data/releases.ts` contains all four releases with complete `platforms` arrays and covers imported as `ImageMetadata`, ready for Phase 3 to consume.
- `npm run build` produces a clean build with Sharp-processed cover art for all four releases and a valid `dist/404.html`.
</success_criteria>

<output>
Create `.planning/phases/02-brand-data-base-layout/02-02-SUMMARY.md` when done.
</output>
